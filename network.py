
class network:
	def __init__(self, n):
		(self.nodes, self.incidenceMatrix, self.range) = getGraph(n, 10**16)
	def requestToSend(node):
		for i in range(n):
			if incidenceMatrix[node][i] == 1:
				node.queue =
